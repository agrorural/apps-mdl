extends includes/base
block globals
    - var body_id= "index"
    - var body_class= "theme-default"
    - var page = "Inicio"
    - var meta_descript = "Inicio de la aplicación"

block content
  nav(class="primary" role="navigation")
    div(class="nav-wrapper container")
      a(id="logo-container" href="#" class="brand-logo") Logo
      
      ul(class="right hide-on-med-and-down")
        li
          a(href="#") Navbar Link
      
      ul(id="nav-mobile" class="sidenav")
        li
          a(href="#") Navbar Link

      a(href="#" data-target="nav-mobile" class="sidenav-trigger")
        i(class="fas fa-bars")

  #index-banner.section.no-pad-bot
      .container
        br
        br
        h1.header.center.secondary-text Starter Template
        .row.center
          h5.header.col.s12.light A modern responsive front-end framework based on Material Design
        .row.center
          a#download-button.btn-large.waves-effect.waves-light.secondary(href="#") Get Started

  div(class="fixed-action-btn")
    a(class="btn-floating btn-large red")
      i(class="far fa-edit")
    ul
      li
        a(class="btn-floating red")
          i(class="fas fa-archive")
      li
        a(class="btn-floating yellow darken-1")
          i(class="fas fa-bell")
      li
        a(class="btn-floating green")
          i(class="fas fa-book-open")
      li
        a(class="btn-floating blue")
          i(class="fas fa-bullhorn")
block append scripts
    script.
      const $ = require('jquery');
      require('materialize-css');

      var toastHTML = '<span>I am toast content</span><button class="btn-flat toast-action">Undo</button>';
      M.toast({html: toastHTML});
      console.log($);

      $(document).ready(function(){
        $('.fixed-action-btn').floatingActionButton();
      });